<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Necessária para o funcionamento do ARCore e captura de vídeo. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Exige um dispositivo com câmera traseira funcional. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <!-- Limita a distribuição na Play Store a dispositivos homologados para ARCore. -->
    <uses-feature
        android:name="android.hardware.camera.ar"
        android:required="true" />

    <!-- Exige suporte a OpenGL ES 3.0 para execução dos pipelines de shaders GLSL. -->
    <uses-feature
        android:glEsVersion="0x00030000"
        android:required="true" />

    <application
        android:name=".AetherisApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aetheris"
        android:usesCleartextTraffic="false">

        <!--
            Define o aplicativo como "AR Required".
            O Google Play Services for AR será baixado automaticamente se ausente.
        -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="required" />

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>